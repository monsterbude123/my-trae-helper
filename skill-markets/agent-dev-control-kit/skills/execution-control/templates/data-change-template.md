# 数据变更执行模板

## 变更信息

- **变更类型**: [DDL/DML/文件迁移/API 变更]
- **目标对象**: [具体表名/文件路径/API 端点]
- **执行人**: [Agent/人工]
- **预计时间**: [YYYY-MM-DD HH:MM]

## 1. 影响评估

### 风险等级

- [ ] HIGH — 影响生产数据/跨表关联/无WHERE条件
- [ ] MEDIUM — 单表变更/有明确范围
- [ ] LOW — 新增测试数据/临时表

### 影响范围

```
受影响组件:
- [组件1]: [影响说明]
- [组件2]: [影响说明]

回滚复杂度: TRIVIAL / MODERATE / HARD
```

## 2. 备份计划

```bash
# 备份命令
[具体备份命令]

# 备份位置
[备份文件路径]

# 备份验证
[验证命令]
```

## 3. dry-run 预演（HIGH 必须）

```sql
-- 预演 SQL
EXPLAIN [变更语句];

START TRANSACTION;
[执行变更语句]
SELECT COUNT(*) AS affected_rows FROM [目标表] WHERE [条件];
ROLLBACK;
```

## 4. 执行记录

| 时间 | 操作 | 结果 | 备注 |
|------|------|------|------|
| HH:MM | 开始执行 | — | — |
| HH:MM | 步骤1 | SUCCESS/FAIL | — |
| HH:MM | 完成 | SUCCESS/FAIL | — |

## 5. 验证结果

- [ ] 数据一致性校验通过
- [ ] 业务功能冒烟测试通过
- [ ] 性能回归检查通过

## 6. 回滚方案（如需）

```bash
# 回滚命令
[具体回滚命令]

# 回滚验证
[验证命令]
```