# V11 自检报告 — 08-real-verify — 角度 8 workflow 可执行性 — 2026-08-12 22:00

## 1. 检查元数据

| 字段 | 值 |
|------|-----|
| stage | `08-real-verify` (Stage 3.5 Real Verify) |
| 角度 | 8 — workflow 可执行性 |
| 检查时间 | 2026-08-12 22:00 |
| 上次检查 | `2026-08-12-21-07-implement.md` (角度 7 — references 时效) |
| 进度文件 | `.last_stage.txt` |
| 检查对象 | `skills/08-real-verify/` 全部文件 |
| 旁路并行 | 总编排器 SKILL.md / constitution.md / glossary.md / scripts/ 抽检 |

---

## 2. 发现清单

### 2.1 真问题(P0/P1, 需修复)

| # | 问题 | 证据 file:line | 质疑性校验 |
|---|------|---------------|----------|
| 1 | **workflow 5 个工作流缺乏"输入物 / 输出物 / 失败兜底 / 验证手段" 四列显式标注** —— `workflows/five-project-verify.md` 5 个工作流(Web/Tauri/CLI/Library/Backend)都用 `bash` 示例串了具体命令,但每个 Step 旁未标注"输入物是什么(由谁来)/ 输出物的字节级阈值/ 失败时具体降级动作"。SKILL.md §铁律 4 说"5 类项目启动验证",但读者必须自己对照 README.md 反推哪步算"输出物"。属于"伪步骤"边缘——命令齐全,但缺少执行者与判读口径。 | `skills/08-real-verify/workflows/five-project-verify.md:11-122`(整段 5 个工作流) | §1.1 ✅ §1.2 ✅ §1.3 ✅ §1.4 ⚠️(修复成本 中等,加 1 张 4 列表行 或 5 个 blockquote; 但未阻塞执行,标 P1) |
| 2 | **workflow 阻塞处理没有"3 次失败升级"或"上报到主上下文"的具体动作** —— `workflows/five-project-verify.md:128-137` 给出了 `blocker:` 5 字段 yaml 模板,但没说这 5 字段**写到哪个文件**(应该是 `.state-card.md` 还是独立 `docs/blockers/{ts}.md`)、也未说"3 次尝试后是否升级到用户决策"。SKILL.md §铁律 5 仅说"5 字段阻塞报告 + 状态卡 blocked",但未定义"blocked 后下一步"——给主上下文留了一个判定真空。 | `skills/08-real-verify/workflows/five-project-verify.md:126-137` + `SKILL.md:34-37`(铁律 5) | §1.1 ✅ §1.2 ✅ §1.3 ✅ §1.4 ⚠️(P1;扩展 workflow §阻塞处理 5 行;影响 §11 ≤10 铁律约束) |
| 3 | **workflow 没有标注"状态卡字段更新触发时机"** —— workflow 5 步串完都没明说"Step X 完成后,改 `.state-card.md` 的哪个字段"。`stage-gate.py` 与 `state-card-validator.py` 在 `scripts/` 存在(`stage-gate.py:13-43` 列出 `card_type/card_id/current_stage/stage_status/updated_at/updated_by/health/artifacts/gate_result/next_stage/actor/duration_minutes/notes` 13 字段),但 workflow 未指明何时填这些字段的哪几个。主上下文执行 Real Verify 时,无法判断"我现在该写哪一字段"。 | `skills/08-real-verify/workflows/five-project-verify.md`(全 146 行,0 处提到 `state-card` / `artifacts` / `next_stage`) | §1.1 ✅ §1.2 ✅ §1.3 ✅ §1.4 ⚠️(P1;在 workflow 末尾加 1 张"Step → 状态卡字段"映射表 ≈10 行) |

> **P0 数 = 0**;**P1 数 = 3**(均未阻塞执行,但降低 main-context 可读性 + 增加误判风险)。

### 2.2 候选修改方案(只读不写, 等用户拍板)

| # | 改动 | 改在哪 | 价值 | 风险 |
|---|------|-------|------|------|
| 1 | 给 `workflows/five-project-verify.md` 5 个工作流统一加 4 列表格(输入物 / 输出物 / 失败兜底 / 验证手段)。 | `skills/08-real-verify/workflows/five-project-verify.md`(每段嵌 1 张 4 列表) | 中 | 低(只增不改;`<150 行` 约束可能破,需打包到附件或拆子文件) |
| 2 | 在 workflow §阻塞处理 后追加 "升级路径":3 次未解决 → 主上下文提示用户决策(接受风险/等待修复/显式豁免);参照 `references/blockage-report.md:18-39` 已有的 `├─ 用户决策` 流程,但 workflow 没引用。 | `skills/08-real-verify/workflows/five-project-verify.md:128-137` + 引用 `references/blockage-report.md` | 中 | 低 |
| 3 | 在 workflow 末尾追加"Step → 状态卡字段"映射:Step 1 → `stage_status=PASS`；Step 3 → `artifacts=[{png,size}]`；Step 4 FAIL → `health=🔴 blocked` + `gate_result=FAIL`。 | `skills/08-real-verify/workflows/five-project-verify.md:145-146` 附近 | 中 | 低(可选项,引用 `state-card-protocol.md` 而非内联,避免突破 150 行) |

### 2.3 不修改(否决)

| # | 提议 | 否决理由(质疑性校验) |
|---|------|--------------------|
| 1 | 把 workflow 5 个项目类型命令合并为 1 个通用 bash 脚本(`scripts/real-verify-projects.sh`),消除 5 段 bash 重复 | §1.4 ❌ — 不是问题,是"为简化而简化"。5 段命令本身差异极大(Web 用 pnpm,Tauri 用 cargo tauri,CLI 跑 end-to-end),强行合并会引入 dispatch + 反射,反而违背 PonyTail 简单原则 |
| 2 | 在 SKILL.md §铁律 加第 7 条:"workflow 必须标 4 维度" | §1.3 ❌ — 这是 stage-level 工作流规范,挂到 stage skill 铁律会膨胀 Article XI ≤10;改放 `references/workflow-writing-conventions.md`(如不存在则新建,挂 SKILL.md §参考索引)更对位 |
| 3 | 改 `workflows/five-project-verify.md:55` 的 `ps aux | grep tauri | grep -v grep` 为跨平台命令(`tasklist | findstr tauri`) | §1.1 ❌ — workflow 是示例性模板,可被项目级 project-rules 覆盖;Windows ps 等价可写但属于项目栈(stack.md)职责,不是本 stage 必修。AGENTS.md §技术栈已说 windows,且 `.trae/rules/stack.md` 不在 stage 08 修复范围 |
| 4 | 把 `references/visual-evidence.md` 的 PIL 3 层校验抽成必跑 step | §1.2 ❌ — `visual-content-check.py` 已是 stage frontmatter 声明的 scripts(可被 main-context 调用);升级到 workflow 步骤会模糊"启动验证"边界(§1.3 重叠于 Stage 4 Review acceptance-discipline) |

---

## 3. 总管视角评价

| 项 | 评价 | 证据 |
|----|------|------|
| **总编排器路由** | ✅ | SKILL.md frontmatter `stage_config.real-verify → skills/08-real-verify/SKILL.md` + `skills: [visual-evidence-discipline, screenshot, playwright-best-practices]` + `stages: [3/implement]` 一致;总编排器 SKILL.md §1 行 165-168 同步声明。 |
| **16 Articles 兼容性** | ✅ | Article V(可验证主张)/X(证据强制)/XIII(可见产品)/XV(障碍诚实)全部在 SKILL.md §铁律 1-6 落地;无矛盾。 |
| **glossary 术语对齐** | ✅ | "Real Verify / Visible Product / Obstacle Honesty / visual-content-check" 均在 glossary.md 一/二节定义;无新造术语。 |
| **Article XVII 影响** | ✅ N/A | Article XVII 是 Secret Redaction,与本 stage(可见产物 + 截图)无关;若涉及截图含 key,属上层 acceptance-discipline 管辖。 |
| **13 rot 扫描覆盖** | ✅ | `stage-gate.py` + `visual-content-check.py` + `dist-hash-check.py` 三脚本在本 stage frontmatter 声明且脚本均实存;rot-scan(10)将在下 stage 触发。 |
| **外部 skill 依赖一致性** | ✅ | SKILL.md frontmatter `skills: [visual-evidence-discipline, screenshot, playwright-best-practices]` 与总编排器 §1 行 168 一致;SKILL.md 正文未引用新外部 skill。 |

---

## 4. 整体评分(5 项 1-10)

| 维度 | 评分 | 说明 |
|------|------|------|
| **workflow 输入物/输出物标注** | 5/10 | 命令齐全但 4 维度未列,主上下文需反推 |
| **workflow 失败兜底完整度** | 6/10 | 阻塞报告模板齐,缺升级路径 |
| **状态卡字段映射** | 4/10 | workflow 0 处提到 state-card;脚本有但 workflow 未引用 |
| **跨文档一致性(脚本真实 + 外部依赖)** | 9/10 | 3 脚本实存 + 3 外部 skill 全部对得上总编排器;扣 1 因 SKILL.md §铁律 4 vs workflow §5 实际项目分类命名微差 |
| **总管视角合规度** | 9/10 | 16 Articles + glossary + Article XVII + 13 rot 全面对齐 |
| **加权平均** | **6.6/10** | 偏向"够用,需微调" |

> 改善优先级(若要采纳):P1#3(状态卡字段映射) > P1#2(升级路径) > P1#1(4 维度表格)

---

## 5. 下次研究角度建议

- 下次从 **角度 9 — 质疑性校验应用** 继续,问题:
  - 08-real-verify SKILL.md §铁律(6 条)是否引用 `references/skeptical-validation-protocol.md`?
  - 任何 ≥ P0 决策(模块强制门禁 / 阻塞升级路径)是否走 §1.4 维度?
  - anti-patterns/`03-skip-screenshot.md` 提到"主上下文亲自 Read",但 SKILL.md 铁律 6 是"主上下文必查";两者边界是否一致?

---

## 6. 必须避免的话术(自检纪律)

- ❌ "为升级而升级" —— §1.4 否决了 4 条候选
- ❌ "完整性不足" —— 全指具体 file:line + 缺字字段
- ❌ "建议优化" —— 给 3 个候选方案 + 风险列
- ❌ "暂时这样" —— 不接受"先合并 bash 脚本,以后再说"型延后
- ✅ 单 stage 深度,无发散
- ✅ 4 维度逐项校验,§1.1-§1.4 全标
- ✅ 不写任何文件除本报告路径
- ✅ 报告路径:`research/auto-audit/2026-08-12-22-08-real-verify.md`(只此一处)

---

## 7. 不修改任何文件 ✅

本次自检**全程只读**:`SKILL.md / README.md / workflows/five-project-verify.md / references/*.md / anti-patterns/*.md / 总编排器 SKILL.md / constitution.md / glossary.md / scripts/*.py`。
**未修改**:`skills/08-real-verify/`、总编排器、任何 stage 相关文件。
**仅产出**:`research/auto-audit/2026-08-12-22-08-real-verify.md`(本报告)。

---

## 8. 进度更新建议(主上下文执行)

```
.last_stage.txt: 07-implement  →  08-real-verify  (本次)
.audit_history.json: 追加 { "stage": "08-real-verify", "angle": 8, "date": "2026-08-12 22:00", "report": "2026-08-12-22-08-real-verify.md" }
下轮角度: 9
```

---

**(完)**
