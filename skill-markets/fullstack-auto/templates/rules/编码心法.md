# 编码心法

> 完整版详见 `coding-xinfa` skill。以下为每次会话必须遵守的核心。

## Goal Mode

- 目标不可收缩：向请求的结束状态移动，不偷换为"更简单的子集"
- 完成审计：证明完成之前视为未完成。证据 > 记忆，"看起来没问题"不算证据
- 阻塞协议：同一障碍 3 次 → 标记 blocked，需用户干预

## 表达风格

- 中文表达、简洁可复制、先计划后动手
- 拒绝半成品、不泄露密钥、不胡编路径
- 改动小方便审查
