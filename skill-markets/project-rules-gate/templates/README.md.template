# Project Rules Skill — 入口

> 本目录由 `project-rules-gate` forge 脚本在 {{GENERATED_AT}} 自动生成。
> 项目名: {{PROJECT_NAME}}

## 职责

本目录是项目级 Rules 的**强制加载入口**,由 `~/.trae-cn/skills/project-rules-gate/` 锻造器自动生成:

- `.trae/rules/` 是单一事实来源(single source of truth)
- 本目录的 `references/` 是同步副本,内容由 forge 从 `.trae/rules/` 复制
- 本目录的 SKILL.md 是入口,任何 agent 必走

## 结构

```
project_rules_skills/
├── SKILL.md                          ← 入口文件(主 agent / sub-agent 必读)
├── README.md                         ← 本文件
├── workflows/
│   └── sub-agent-delegate-load.md   ← sub-agent 委派头部模板
└── references/                       ← 自动同步副本
    ├── stack.md
    ├── paths.md
    ├── git.md
    └── ...
```

## 加载方式

```python
Skill(name="project-rules")
```

## 本项目 rules

{{RULES_LIST}}

详见 SKILL.md §3 路由表(按场景加载)。

## 禁止

```
❌ 跳过本 skill 直接 Read .trae/rules/*.md
❌ 用 grep / Glob 搜 rules(必须走 skill 入口)
❌ 加载全部 rules(按 SKILL.md §3 路由表按需加载)
❌ 直接编辑 references/ 下的文件(改完后会被 forge 覆盖)
```

## 锻造器

```bash
# 重新锻造(同步 .trae/rules/ 到 references/)
python ~/.trae-cn/skills/project-rules-gate/scripts/forge_project_rules_skill.py --project-root .

# 演练(不写文件)
python ~/.trae-cn/skills/project-rules-gate/scripts/forge_project_rules_skill.py --project-root . --dry-run
```

详见 [.trae/rules/README.md](../../../../rules/README.md) 强制入口说明。

---

*由 project-rules-gate v0.1 自动生成*
