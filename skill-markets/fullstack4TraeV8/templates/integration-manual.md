# {模块名} 接入手册

> 🔷 **基石模块** — 其他模块必须遵循本手册的约定才能正确集成。
>
> 基于 Change {NN}-{change-name}，最后更新 {YYYY-MM-DD}。

---

## 一、模块定位

{一句话描述此模块提供什么基础能力，为什么是基石}

## 二、接入步骤

### 步骤 1: {初始化/配置}
```
{代码示例}
```

### 步骤 2: {核心调用}
```
{代码示例}
```

### 步骤 3: {结果处理}
```
{代码示例}
```

## 三、约定规范

| 规范类型 | 约定 | 示例 |
|---------|------|------|
| 错误处理 | {统一异常类型/错误码格式} | `throw new ModuleError('CODE', msg)` |
| 命名规则 | {函数/变量/文件命名约定} | `camelCase` / `PascalCase` |
| 超时/重试 | {超时时间 + 重试策略} | 3次重试，间隔 1s/2s/4s |
| 日志规范 | {日志级别 + 格式} | `[ModuleName] level: message` |

## 四、接口速查

| API | 签名 | 说明 | Published |
|-----|------|------|:---:|
| {apiName} | `({params}) => {return}` | {一句话} | ✅ |

## 五、反例（不要这样做）

### ❌ 反例 1: {错误用法描述}
```javascript
// 错误: {为什么错}
{错误代码}
```

### ❌ 反例 2: {错误用法描述}
```javascript
// 错误: {为什么错}
{错误代码}
```

## 六、最小可用示例

```javascript
// 完整可运行的接入代码
{代码}
```

---

> 此手册由 doc-updater 在 DOC SYNC 阶段基于 spec + contract 自动生成。
> 基石模块判定标准见 [doc-updater 场景 8](../agents/doc-updater.md#场景-8--基石模块接入手册生成v8-new)。
