# fullstack 脚本工具

## spec-validate.py

校验 spec.md 是否符合 BDD 场景格式规范（WHEN-THEN-AND + SHALL + L0-L4 编号）。

使用：
```bash
python scripts/spec-validate.py docs/specs/changes/{change}/specs/{capability}/spec.md
```
